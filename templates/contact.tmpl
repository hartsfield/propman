<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 3.2 Final//EN">
{{template "head"}} 
<html>
  {{template "hamburger" .}}
  {{template "contact" .}}
  {{template "likeButton" .}}
  {{template "footer" .}}
</html>
