<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 3.2 Final//EN">
{{template "head"}} 
<html>
  {{template "hamburger" .}}
  {{template "services" .}}
  {{template "likeButton" .}}
  {{template "footer" .}}
</html>
